<UserControl x:Class="AveCaesarApp.Views.ProductView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AveCaesarApp.Views"
        xmlns:vm="clr-namespace:AveCaesarApp.ViewModels"
        mc:Ignorable="d"
        Height="450" Width="800" FontFamily="Source Serif Pro" FontSize="24" Background="#FF024300" >
    <Grid>
        <TabControl FlowDirection="LeftToRight" BorderThickness="0" Grid.Row="0" Grid.Column="0">
            <TabItem Height="50" Header="{Binding AddOrEditTabText}" IsSelected="True" BorderThickness="0"
                     Template="{StaticResource TabItemTemplate}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="1" Grid.Row="5" Grid.ColumnSpan="2" Margin="0,5" Command="{Binding AddOrEditProductCommand}" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="AddOrEditButtonBorder" Background="White" CornerRadius="10" 
                                        BorderBrush="{StaticResource MainGreenColor}" BorderThickness="3">
                                    <TextBlock x:Name="AddOrEditButtonText" Text="{Binding AddOrEditButtonText}" 
                                               HorizontalAlignment="Center"  VerticalAlignment="Center"  Foreground="{StaticResource MainGreenColor}"  />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsEnabled" Value="True">
                                        <Setter TargetName="AddOrEditButtonBorder" Property="Background" Value="{StaticResource MainGreenColor}" />
                                        <Setter TargetName="AddOrEditButtonBorder" Property="BorderThickness" Value="0" />
                                        <Setter TargetName="AddOrEditButtonText" Property="Foreground" Value="White" />
                                    </Trigger>  
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="ExitButton" Grid.Column="4" Grid.Row="5" Grid.ColumnSpan="2" Margin="0,5" Command="{Binding NavigateToProductsCommand}" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{StaticResource MainGreenColor}" CornerRadius="10" 
                                        BorderThickness="0" BorderBrush="{StaticResource MainGreenColor}">
                                    <TextBlock Text="Назад" HorizontalAlignment="Center"  VerticalAlignment="Center"  Foreground="White" />
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <StackPanel Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Grid.ColumnSpan="2" VerticalAlignment="Center">
                        <Label Content="Название" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBox>
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border BorderBrush="{StaticResource MainGreenColor}" BorderThickness="2" CornerRadius="3" Padding="2">
                                        <TextBox Text="{Binding Path=ProductName, Mode=TwoWay}" BorderThickness="0"/>
                                    </Border>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>
                    <StackPanel Grid.Column="4" Grid.Row="2" Grid.RowSpan="2" Grid.ColumnSpan="2">
                        <Label Content="Количество" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBox >
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border BorderBrush="{StaticResource MainGreenColor}"
                                            BorderThickness="2" CornerRadius="5"  Padding="2">
                                        <TextBox Text="{Binding ProductAmount, Mode=TwoWay}" BorderThickness="0"/>
                                    </Border>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>
                    <StackPanel Grid.Column="4" Grid.Row="0" Grid.RowSpan="2" Grid.ColumnSpan="2" VerticalAlignment="Center">
                        <Label Content="Калорийность" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBox  >
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border BorderBrush="{StaticResource MainGreenColor}" 
                                            BorderThickness="2" CornerRadius="3"  Padding="2">
                                        <TextBox Text="{Binding ProductCalories, Mode=TwoWay}" BorderThickness="0" />
                                    </Border>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Grid.Row="2" Grid.RowSpan="2" Grid.ColumnSpan="2" >
                        <Label Content="Цена" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBox>
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border BorderBrush="{StaticResource MainGreenColor}" 
                                            BorderThickness="2" CornerRadius="3"  Padding="2">
                                        <TextBox Text="{Binding ProductPrice, Mode=TwoWay}" BorderThickness="0"/>
                                    </Border>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
